# WAF Console — custom rules written here at runtime by modsec-rule-manager.js
# This directory is mounted into the Nginx+ModSecurity container as a volume.
# Do not edit files here manually — use the WAF Console UI.
